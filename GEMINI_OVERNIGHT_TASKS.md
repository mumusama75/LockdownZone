# Gemini 整夜执行任务书：封锁区·零号大厦

你负责继续完善当前 UE5 demo，使它更适合作为国内游戏策划／关卡策划求职作品。直接执行下列任务并留下实际成果，不要只写计划。此次用户授权的是本地项目迭代与作品集材料制作，不包含发布、推送远端、购买资产或向外发送文件。

## 目标与时间预算

从开始执行起，按约6–8小时安排工作；这是预算，不是对工期的保证。优先完成可运行的关卡改进与证据，时间不足时削减可选项。每30–60分钟更新一次项目内进度记录。无需等用户逐阶段回复；常规选择自行判断。遇到缺失工具或权限，记录具体阻碍，继续独立任务，不得把未验证内容标记为完成。

明早要能展示：

1. 一个可以从头玩到撤离、保留全部现有系统的版本。
2. 至少3个可被实机证据支持的关卡改进，分别针对教学、空间引导、探索取舍。
3. 一套让招聘者快速理解“设计问题—解决方案—实机结果—验证局限”的中文作品集材料。

## 先确认现状

- 实际项目：`C:/Users/木/Documents/ChatGPT/cv/projects/LockdownZone`。
- `C:/UEProjects/LockdownZoneRepo` 是上述目录的 junction，同一套文件；历史 `C:/UEProjects/LockdownZone` 不使用。
- 引擎：`C:/Program Files/Epic Games/UE_5.8`，UE 5.8.2。
- 先读实际生效的 AGENTS.md（如有）、`README.md`、`DESIGN.md`、`HANDOFF_ANTIGRAVITY.md`、当前源码。
- 本任务书整理时，最新提交是 `7cb58b9`，最新现存 QA 报告为255通过／0失败、54.42秒；这是历史基线，不可复制成今晚的新结果。
- 当前已有 `Play.bat`、`Docs/Screenshots` 和更新后的 README。交接文档中“无Git提交”等旧信息已过时，以实时 Git 状态为准，保护其他 agent 的修改。
- 场景在 C++ 中运行时生成，启动地图是 `/Engine/Maps/Entry`。优先看 `LZGameMode::BuildOfficeLevel`、`LZOfficeDressing.cpp`，不要以编辑器空地图判断关卡丢失。

## 不得破坏的规则

保留办公室醒来→拾取武器→破玻璃与战斗→第4次击杀断电→机房保险丝→配电恢复→东侧撤离；中文HUD与F5重开保留。

未武装时敌人不移动、不攻击，单独拾取手电不能解除保护。手枪容量17、初始3/0，换弹扣除实有备用弹药；保留后坐力与瞄准减弱。手电E拾取/F切换。B打开3×2六格背包；弹药每格30、医疗包使用恢复最多35、满包不吞物、任务物不占普通格；整理背包不暂停世界。不要扩大容量来回避探索取舍。

保留已经修复的斧头初始隐藏、武器可见性、家具尺度、多材质槽和手枪父材质依赖。`Content/Weapons/Rifle/Materials/M_Weapon.uasset` 是手枪所需资源，不能删除。

默认不重构 `LZCharacter`、`LZInventoryTypes`、`LZEnemy` 的核心状态逻辑。发现会阻断本轮的缺陷可以做最小修复并补验证；复杂跨系统问题写入给 Astra 的清单。可修改关卡布置、局部灯光、场景标识、提示文案、必要的场景资源与相关QA取景。

本夜不新增武器种类、技能树、任务系统、仓库、联网、存档、程序化关卡或大型背包重写；不更换引擎版本或切换到高成本渲染配置。优先复用现有合法资产，不下载来源不明资源。

## 阶段0：建立可回退基线（约30分钟）

1. 检查工作树、现有提交和是否有其他 agent 正在写同一文件。同一份工作目录只允许一个写入者、一个构建/QA任务。冲突时转做只读分析和文档。
2. 有未提交内容时先记录并保护，不能用 reset/clean 抹掉；如需快照，只处理明确属于本项目的文件，不把整个上层仓库提交进去。
3. 运行现有构建、QA；保存本次基线报告、日志和原始截图到 `Docs/Overnight/Baseline/`，之后不覆盖。二进制与缓存不进Git。
4. 若可控制游戏，正常模式走一遍；记录工具能力。无法实际输入时，说明自动流程能验证什么、不能验证什么，不伪造人工体验。
5. 写 `Docs/Overnight/Progress.md`：开始时间、基线提交、当前改动、阶段状态、阻碍。

验收：真实基线可运行，资料已保存，至少列出3个有截图/代码位置依据的关卡问题。若基线失败先修复阻断问题，压缩后续美术范围。

## 阶段1：用一张图明确关卡设计（约40分钟）

从实际布局和坐标绘制可编辑SVG平面图，而非凭文字想象。包括墙体/可通行区域、出生点、首武器、玻璃、主要敌人位置、保险丝、配电箱、出口、补给与计价物。标明方向和区域名称。

用线型和文字区分主线路径、可选搜索、供电返程；标出至少3个决策点和关键视线。图应与实机关卡一致；若只做示意图，明确注明不按比例。

写一页设计简报，限定3个设计命题：

- 开场：玩家如何先观察威胁，再理解工具与资源限制？
- 中段：为何值得偏离主线搜索，代价是什么？
- 断电后：怎样利用先前空间记忆找到目标并返回撤离？

每个命题写“现有问题、拟改内容、预期玩家行为、验证方式”。后续修改都对应其中一项，不为填满场景而装饰。

## 阶段2：开场教学与首次交战（约70分钟，必做）

保持开场玩法顺序，通过武器摆放、局部明暗、家具朝向和简洁提示，建立“窗口威胁→武器→弹药→出口”的阅读顺序。

- 保证出生视角看得见窗后威胁，未拾取武器时不出现第一人称巨物遮挡。
- 武器和手电能辨识，靠近时交互提示完整；不要以大面积发光代替模型轮廓。
- 首次交战区域至少有可理解的掩体和退路，桌椅不是碰撞陷阱；移位后核查敌人能否到达、玩家能否通过。
- 3/0、17发容量与补弹需求在HUD和场景中一致。不增加长篇弹窗教程。

验收：拍出生点、武器观察位、首次交战位3张可复用机位的前后对比；实际检查未武装保护、三种拾取、射击、换弹、玻璃。把“可看见”与“新玩家真的注意到”分开记录，后者需要后续真人测试。

## 阶段3：断电搜索与返程引导（约80分钟，必做）

重点处理机房入口、保险丝所在位置、配电设施、撤离门洞四处。

- 每个主要区域有不同的形状/布局地标，不只靠颜色区分；沿用稳定的方向与区域命名。
- 在真实岔路放合理的导向，提供“走对了”的中途确认；避免全程铺满箭头。
- 断电时保留通路和近处障碍轮廓，手电提升主动搜索能力；避免完全漆黑或全场像开灯一样亮。
- 保留正常、断电、恢复供电三种可区分状态；修正桌面强热点，不能通过抬亮整个场景消除黑暗。
- 让玩家看得清保险丝、配电箱正面和撤离门洞；在现有碰撞和交互逻辑外增强外观，不只保留彩色方块/平板。
- 返程至少有一个复用前期认识的地标或视线关系。默认通过布局/引导完成，不新增复杂动态门系统。

验收：同一机位三种供电状态对比，手电开/关对比；主线完整可达，关键交互射线不被装饰挡住；截图展示完整门洞和路径，不只贴脸拍按钮。

## 阶段4：让“搜打撤”存在具体选择（约60分钟，必做）

使用现有北侧绕行、南侧机房深搜、6格背包及三件计价物，改善路线和资源摆放。不要为增加深度额外造一套系统。

至少形成两处玩家能提前理解的选择，例如：

- 从主路看见可选补给，但拾取需要绕过办公隔断后再回主路。
- 主线保险丝附近能看见更深处的高价值备件，玩家可以直接离开或多走一段搜索。
- 医疗、弹药和大件战利品竞争容量，界面能解释为什么装不下。

具体采用哪种方案由现有空间决定。奖励不能藏到只有开发者知道；取舍要能说清“看见什么、付出什么、得到什么、如何返回”。若此时敌人已全部被清理，代价应描述为距离/时间/容量，不能虚构持续战斗风险。没有经过自然游玩验证，不要给路线贴“高风险/低风险已平衡”标签。

验收：完成一张路线取舍表，列触发位置、可见线索、代价、收益、返回关系；验证直接撤离和携带额外战利品两种路径。收益差异来自实际库存与结算，不手工填一个假结果。可以调整物资位置，但默认保留已有价值和数量。

## 阶段5：做成可阅读的策划作品集（约75分钟，必做）

输出 `Docs/Portfolio/`，复用本轮成果，不用大段宣传文案填页。至少包含：

1. `Portfolio.md`：6–8页结构的内容稿，招聘者两分钟内能理解核心设计。
2. `index.html`：本地可打开、无外部CDN依赖的中文展示页，带打印样式；小屏/常规桌面无明显裁切。若工具支持，打印为PDF并逐页检查；否则如实交付HTML，不阻塞关卡工作。
3. `LevelPlan.svg`：与最终布局对应的平面图。
4. `RouteChoices.md`：两条可选搜索路径的选择与代价。
5. `Playtest.md`：本轮实际验证方式、观察、局限，以及真人测试记录表。
6. `ResumeBullets.md`：3条可用于简历的项目描述和约90秒讲解稿。
7. 原始实机截图与标注图分开保存，至少6张有设计用途的图；保留原始文件，标注不得伪装成游戏内表现。

作品集建议顺序：项目定位/职责→平面图/体验节拍→开场教学→搜索取舍→断电与空间复用→三项迭代对比→验证与反思→来源和后续计划。

对至少3项改动使用“问题截图→设计假设→修改后截图→实际检查/仍待测试”的结构。提出预期是允许的，声称玩家行为已改变必须有实际测试依据。

写清 UE 官方、Kenney、自制内容与AI辅助的边界；不要把模板资产说成原创模型，不要把本轮所有生成代码写成用户独立手写。简历可描述用户实际承担的设计目标、决策和验证组织，注明开发由AI辅助，不编造团队人数、提升百分比或玩家样本。

不能用机器人流程54秒作为玩家通关时长；不能声称“经多人测试”“帧率稳定60”“10–15分钟流程”而无证据。设计节拍可以写目标时长并明确“待真人验证”。

## 阶段6：最终回归与收尾（至少预留60分钟，必做）

到剩余约60–90分钟时停止增加范围，专注修复和交付。整夜上限将到时，以最后可运行版本为准；不得为赶时间保留已知破坏主线的半成品。

每一批代码/资产修改完成后进行一次对应的构建、运行与截图检查；同批完成后再测，避免每改一行全量回归。最终运行完整QA，保持原有检查语义，不得删除/放宽断言来掩盖退化。布局改变导致旧取景点无效时，可修正QA位置或寻路依据，并记录原因。

```powershell
Set-Location 'C:\UEProjects\LockdownZoneRepo'
powershell -ExecutionPolicy Bypass -File .\Scripts\Build.ps1
powershell -ExecutionPolicy Bypass -File .\Scripts\Play.ps1 -QA
powershell -ExecutionPolicy Bypass -File .\Scripts\Play.ps1
```

检查：

- 编译成功、正常模式能启动，主线从开场到撤离及F5重开完整。
- 开场未武装保护；武器、后坐、换弹、手电与背包交互无退化。
- 满包拒收不丢物，任务保险丝不因背包满而受阻，战利品结算一致。
- 路径碰撞、门洞、拾取范围、中文提示、黑暗可读性逐项有证据。
- 记录完整QA的实际通过/失败数、时间、日志路径及截图；新增检查单独列清楚。
- QA冻结敌人阶段与正常敌人运行阶段分开说明。没有完成自然游玩就标“待验证”。

最后生成 `Docs/Overnight/REPORT.md`：完成项、改动文件、三个设计收益及证据、构建/回归结果、未完成项、下一步最多5条、给Astra的复杂问题。更新README与DESIGN中已发生变化的内容，不覆盖其他agent新增的正确资料。

若基线已有Git且无人并发改同一文件，可对本轮明确文件做阶段性本地提交；不要使用上层仓库无范围的 `git add .`，不要推送远端。若存在混合修改无法分清，保存diff/清单并明确说明，不代替别人提交。

## 执行纪律和卡住处理

- 同一技术问题连续两次有实质差异的修复仍失败，或耗时超过约20分钟：保留日志和最小复现，撤回自己该项未完成修改，转做独立任务。若是主线阻断问题，则以恢复本轮前的可运行状态优先。
- 只撤回自己有记录的修改；不恢复整个目录、不删除陌生资产、不清空Saved来掩盖日志。
- 不关闭其他人的未保存编辑器会话。需要关闭本轮启动进程时确认它属于本任务。
- 只读日志、坐标推导和截图不能代替实际游玩；工具不可用要披露。
- 不使用运行时永久冻结敌人、无限弹药或自动完成目标来“保证通关”。测试辅助只留在明确的QA入口。
- 不为了耗完预算反复重测已经通过且未修改的内容。必要结果完成后可提前收尾。
- 最后给用户简短中文汇报，并指向作品集入口、最终报告和运行方式；任务完成与尚待真人测试的部分分开列。

## 可选项：仅在全部必做通过且仍有余量时

按价值选择一项：关键交互物轻量模型优化、用现有合法音效补充供电反馈、制作2–3分钟真实流程录像。录屏工具不可用则给出按实机节点编写的录制脚本，不伪造视频。

独立打包不作为本夜硬指标；工具链现成且还有充足时间才尝试，失败不破坏开发运行版本。

现在开始阶段0。短暂说明你的执行顺序后直接行动，持续到交付或遇到无法继续的真实阻碍。
