# 《封锁区：零号大厦》Antigravity 开发交接

整理日期：2026-09-23。最新已完成的构建与运行验证日期：2026-09-22。

## 1. 接手目标与项目位置

继续开发 UE5 单人末世办公楼 FPS 搜打撤垂直切片，目标用于国内关卡策划作品集。现阶段是可运行的玩法与初步美术整合版本，仍需打磨角色、美术、交互反馈和首次体验。请在现有实现上迭代。

| 项目 | 路径或说明 |
| --- | --- |
| 实际 Git 根目录 | `C:/Users/木/Documents/ChatGPT/cv` |
| 唯一开发项目 | `C:/Users/木/Documents/ChatGPT/cv/projects/LockdownZone` |
| 项目文件 | 上述目录内 `LockdownZone.uproject` |
| ASCII 路径 | `C:/UEProjects/LockdownZoneRepo`，已确认是指向上述项目的 junction，两者是同一套文件 |
| 历史副本 | `C:/UEProjects/LockdownZone`，不要以此为开发源，也不要反向覆盖仓库项目 |
| 引擎 | 本机 `C:/Program Files/Epic Games/UE_5.8`，使用 UE 5.8.2 |
| 先读文档 | 项目根目录 `README.md`、`DESIGN.md`、本交接说明 |

2026-09-23 检查时 Git 仍没有任何提交，项目显示为未跟踪文件；仓库还有其他工作内容。接手时重新检查状态，保留现有文件，不要对整个仓库执行清理或回滚。不要假定这些成果已经推送到远端。

## 2. 用户明确要求

- 保留中文 HUD 和已有完整玩法流程。
- 玩家未武装之前，敌人不能移动或攻击；单独拾取手电不算武装。
- 射击需要后坐力，场景有可拾取手电筒。
- B 打开背包，容量有限，可存道具与战利品。
- 统一资产尺度与材质，逐步替换灰盒墙、门和机柜，改善灯光、破坏痕迹及视觉引导。
- 可以使用 UE 5.8 自带模板和项目已导入的 CC0 资产。
- 每轮代码或资产改动后编译、实际运行、截图检查，阶段完成后汇报。

上述要求已有阶段性实现；后续建议不代表用户已经要求新增所有扩展系统。

## 3. 当前可玩流程

1. 西侧办公室醒来，未持武器，透过玻璃观察感染者。
2. E 拾取消防斧、手枪；有短暂检视表现。手枪按格洛克17玩法规格：容量17发，初始弹匣3发、备用0发。视觉模型使用 UE 官方模板手枪，不能称为准确的格洛克17模型。
3. 搜索弹药，破坏玻璃，战斗；敌人有受伤与死亡反馈。第4次击杀触发断电。
4. 到南侧机房寻找保险丝，再到北侧配电点恢复供电。
5. 到东侧出口 E 撤离，结算带出价值；死亡或结束可 F5 重开。

另有北侧办公绕行和南侧机房深搜，可选拾取医疗包、电子零件和服务器备件。未验证首次体验时长，不应宣称已达到10–15分钟或完成战斗平衡。

## 4. 操作与已实现系统

| 输入 | 功能 |
| --- | --- |
| WASD / 鼠标 / 空格 | 移动 / 观察 / 跳跃 |
| 左键 / 右键按住 | 当前武器攻击 / 手枪瞄准 |
| 1 / 2 | 切换已拾取的斧 / 手枪 |
| R | 换弹，从背包实有弹药扣除 |
| E | 场景拾取、主线交互、撤离；背包内改为使用医疗包 |
| F | 开关已拾取的手电 |
| B | 开关背包 |
| 方向键 / Delete | 背包选格 / 丢弃整件或整堆，不可找回 |
| F5 | 重开本局 |

### 射击与手电

手枪具有镜头抬升、轻微横向偏移、枪身后移/上抬/回弹，瞄准时减弱，停止射击后恢复且保留新的鼠标输入。子弹命中判定发生在本次后坐力施加之前。空仓、换弹和近战不触发手枪后坐力。

手电在开场手枪桌上，E 拾取后自动开启，F 开关；可与斧或枪同时使用。当前没有电池消耗，不占普通背包格。相机附着聚光灯：2.2 cd、1800 cm、内锥12°/外锥25°、4800 K、有阴影。第一人称武器采用独立灯光通道及弱补光，避免被近距手电直射成白块；调整时检查开/关手电同视角对照。

### 六格背包

3列×2行，共6格，实际物品条目驱动容量、备用弹药与价值，不是只展示计数的界面。

| 类型 | 规则 |
| --- | --- |
| 弹药 | 每箱12发，每格上限30发；自动先补旧堆再占新格；换弹耗尽一堆释放该格 |
| 医疗包 | 每件1格，拾取后存储；E使用最多恢复35生命；满血时不消耗 |
| 电子零件 | 每件1格，价值120 |
| 服务器备件 | 每件同一行连续2格，价值500 |

满包或缺少连续空间时拾取失败，场景物品保留，库存不发生部分增加。武器、手电、任务保险丝独立显示且不占六格。三件计价物总值1120、占5格，弹药与医疗包也占格，形成容量取舍。

打开背包不暂停世界，角色仍会受伤；阻止移动、视角转动、跳跃、攻击、瞄准、换弹、切枪及场景交互。E只处理包内使用。关闭菜单只解除它自己施加的输入锁，不能解除死亡/结算锁。死亡和结算会关闭菜单，F5重开清空本局背包。

当前是键盘操作的 Canvas HUD，未实现鼠标拖拽、物品旋转、分堆、仓库或跨局存档。Delete直接删除条目，不生成可再次拾取的地面掉落。

## 5. 美术现状与已解决的问题

- 已导入 UE 官方手枪、Mannequin 及 Kenney CC0 家具。Manny 是临时感染者表现；斧头和部分交互道具仍有灰盒感。
- 墙、工业门、100×80×220 cm 服务器机柜已使用轻量模块；保留多材质槽。
- 已加入 ISM 地毯、天花格栅、纸屑、破损、水痕、倒椅，运行时固定曝光，区分正常/断电/恢复供电。
- 场景内分区标识使用 ASCII，中文主要通过 HUD 显示。
- 既往资产审计24个网格、56个材质槽，0空材质/0默认材质；这是资产引用审计，不能等同最终美术验收。

**开场巨物根因已经修复：** 第一人称斧头子组件 `MeleeHead` 在构造时未可靠继承父组件隐藏状态，导致未持武器也出现在镜头前。现已显式隐藏并在 BeginPlay 同步武器可见性。先前怀疑 `LZWeaponPickup_1` 约52×93×239 cm 的 Actor 包围盒不能直接证明手枪巨大，因为包含交互/附属组件；当前手枪拾取网格按约20.4 cm归一化。不要仅凭总包围盒重新调错尺寸。

**材质依赖已补齐：** 手枪 `MI_Weapon_Pistol` 依赖 `/Game/Weapons/Rifle/Materials/M_Weapon`。该文件已从本机 UE 官方模板复制到项目同路径，最终回归不再报缺失父材质。不要因为目录名为 Rifle 就删掉它。模块合并曾退回默认棋盘材质，现也已修复。

剩余可见问题：消防斧较粗大；Manny、备件和撤离交互外观仍偏临时；手电近距桌面热点偏亮；配电箱/撤离口现有 QA 近景适合验证交互，缺少作品集所需的完整空间构图。

## 6. 代码导航

下列源文件均在 `Source/LockdownZone/`：

| 文件 | 职责 |
| --- | --- |
| `LZGameMode.h/.cpp` | 场景生成、玩法阶段、武器解锁、击杀断电、保险丝、供电、撤离和重开；优先看 `BuildOfficeLevel` |
| `LZOfficeDressing.cpp` | `ALZGameMode` 的模块墙及办公环境布置实现 |
| `LZCharacter.h/.cpp` | 玩家输入、武器动作、后坐力、手电、背包数据与输入锁 |
| `LZInventoryTypes.h` | `ELZInventoryItemType`、`FLZInventoryEntry` |
| `LZHUD.h/.cpp` | 中文普通HUD、背包与结算；Canvas绘制，设计尺寸1280×720并缩放 |
| `LZLoot.h/.cpp` | 弹药/医疗/计价物拾取与空间不足提示 |
| `LZWeaponPickup`、`LZFlashlightPickup` | 武器、手电的场景拾取物 |
| `LZEnemy`、`LZBreakableGlass` | 敌人、玻璃 |
| `LZPowerInteractable`、`LZExtraction` | 供电主线交互、撤离 |
| `LZSliceQA.h/.cpp` | 自动流程回归、输入检查与截图 |

`Config/DefaultInput.ini` 保存键位，`Config/DefaultEngine.ini` 设定地图/模式及渲染。当前使用 DX11，未启用 Lumen/虚拟阴影；不要直接套用高成本渲染配置。

**场景由 C++ 在运行时生成，启动地图为 `/Engine/Maps/Entry`，默认模式为 `LZGameMode`。** 编辑器里只看到空启动地图并不等于项目丢失。源码中还存在旧灰盒和旧谜题路径，修改前先确认实际运行分支。

背包关键接口：`TryStoreItem`、`CanStoreItem`、`GetInventoryEntries`、`GetInventoryItemAtSlot`、`UseSelectedInventoryItem`、`DiscardSelectedInventoryItem`、`GetInventoryStatusText`。容量、备用弹药、带出价值均从条目派生，勿再引入重复且易失步的计数器。

## 7. 构建、运行与验收

先确认并关闭占用本项目模块的编辑器/游戏，再在 PowerShell 执行：

```powershell
Set-Location 'C:\UEProjects\LockdownZoneRepo'
powershell -ExecutionPolicy Bypass -File .\Scripts\Build.ps1
powershell -ExecutionPolicy Bypass -File .\Scripts\Play.ps1 -QA
# 回归完成后正常游玩
powershell -ExecutionPolicy Bypass -File .\Scripts\Play.ps1
```

构建目标为 `LockdownZoneEditor Win64 Development`，需要 VS C++ 工具链和 Windows SDK。脚本可用 `-EngineRoot` 指定引擎。Play不会隐式编译。脚本会校验 junction，遇到目标冲突不会覆盖。

最新实际验证：**255项通过、0项失败，返回码0，54.49秒，23张截图**。

| 证据 | 项目内相对路径 |
| --- | --- |
| 代码构建日志 | `Saved/Logs/Build-20260922-234821-354.log` |
| 补齐材质后构建 | `Saved/Logs/Build-20260922-235626-399.log` |
| 最终回归运行日志 | `Saved/Logs/Play-20260922-235634-971.log` |
| QA报告 | `Saved/LZSliceQA_Report.txt` |
| 实机截图 | `Saved/Screenshots/WindowsEditor/SliceQA_*.png` |

背包截图为 `SliceQA_01c_InventoryEmpty.png`、`SliceQA_07c_InventoryLoot.png`、`SliceQA_07d_InventoryFull.png`；其他截图覆盖开场、武器、后坐、玻璃、战斗、断电、手电开关、保险丝、供电、撤离和重开。

QA在部分阶段临时冻结敌人以确定性检查交互，不等于自然战斗/首次体验/难度平衡验收。接手后先复跑基线；每次功能变更再做对应检查，不必为本交接文档本身重新编译。

## 8. 建议后续优先级（待结合用户下一步要求）

1. 正常游玩整条流程：观察首武器引导、敌人解锁时机、资源压力、断电路线和背包整理时机，记录问题后再调数值。
2. 改善斧头、感染者及关键交互物的表现，控制近距热点；保持现有尺度、碰撞、交互距离和中文提示。
3. 加强办公楼叙事与路径地标，尤其配电箱、机房入口、撤离门洞；不要用装饰堵住通路或开场观察窗。
4. 打磨命中、受伤、换弹及交互音效/动画；检查瞄准和后坐恢复手感。
5. 做适合作品集的宽景截图、路线图、设计说明和真实首次体验记录；独立打包仍未完成。

先验证现有成果，再按用户指定方向推进。不要把建议中的内容描述为已经完成，也不要把本轮未要求的仓库、多人、持久化等系统纳入默认开发范围。

## 9. 可直接发送给 Antigravity 的接手指令

> 请接手 UE5 项目《封锁区：零号大厦》。先阅读项目根目录 `HANDOFF_ANTIGRAVITY.md`、`README.md` 和 `DESIGN.md`。实际项目是 `C:/Users/木/Documents/ChatGPT/cv/projects/LockdownZone`；`C:/UEProjects/LockdownZoneRepo` 是同目录 junction，历史 `C:/UEProjects/LockdownZone` 不作开发源。当前已有完整办公楼搜打撤流程、后坐力、可拾取手电、B键六格背包及中文HUD；最新QA为255通过/0失败。先确认路径和当前文件状态，正常体验并复跑现有构建/QA，汇报基线及下一阶段重点，再结合我的下一条要求继续开发。保持未武装前敌人静止，不要破坏现有流程；每轮代码或资产改动后编译、运行并查看截图。源码目前未提交，保留所有已有改动。
